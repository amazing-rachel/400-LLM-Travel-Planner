export const GUEST_STORAGE_KEY = 'guest_itineraries';
/** Set to '1' only after successful generate on Trip Input (guest), before redirect to Saved. */
export const GUEST_SHOW_SAVED_KEY = 'guest_show_saved_on_visit';
/** Dispatched when user clicks Saved Itineraries in nav so the page reloads guest list (empty). */
export const GUEST_SAVED_NAV_EVENT = 'guest-saved-nav';
